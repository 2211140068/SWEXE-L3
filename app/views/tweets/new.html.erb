<h1>新しいツイート</h1>

<%= form_with(model: @tweet, local: true) do |form| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "エラー") %> prohibited this tweet from being saved:</h2>
      <ul>
        <% @tweet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :message %>
    <%= form.text_area :message, maxlength: 140 %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= link_to '戻る', tweets_path %>
